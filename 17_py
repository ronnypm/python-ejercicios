#startswith es una función integrada en Python para las cadenas (str). Sirve para verificar si una cadena comienza con un prefijo específico.
startwith("letra")


# La función isinstance es una función incorporada en Python que se utiliza para verificar si un objeto pertenece a un tipo específico (o a una clase específica).

strings_only = [item for item in items if isinstance(item, str)]
print(strings_only)